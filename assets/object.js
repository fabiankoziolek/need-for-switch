"use strict";

export class Object {
  constructor(x, y, width, height, rotation, context) {
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
    this.rotation = rotation;
    this.context = context;
  }
}
